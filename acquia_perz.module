<?php

use Drupal\Core\Entity\EntityInterface;

/**
 * Implements hook_entity_insert().
 */
function acquia_perz_entity_insert(EntityInterface $entity) {
  \Drupal::getContainer()
    ->get('acquia_perz.export_content')
    ->exportEntity($entity);
}

/**
 * Implements hook_entity_update().
 */
function acquia_perz_entity_update(EntityInterface $entity) {
  \Drupal::getContainer()
    ->get('acquia_perz.export_content')
    ->exportEntity($entity);
}

/**
 * Implements hook_entity_predelete().
 */
function acquia_perz_entity_predelete(EntityInterface $entity) {
  \Drupal::getContainer()
    ->get('acquia_perz.export_content')
    ->deleteEntity($entity);
}

/**
 * Implements hook_entity_translation_predelete().
 */
function acquia_perz_entity_translation_predelete(EntityInterface $translation) {
  \Drupal::getContainer()
    ->get('acquia_perz.export_content')
    ->deleteTranslation($translation);
}
